# The Windows recovery task uses an outbound reverse SSH tunnel. Keep the
# listener on cloud loopback; Nginx is the only public HTTPS entry point.
Match User admin
    AllowTcpForwarding remote
    GatewayPorts no
