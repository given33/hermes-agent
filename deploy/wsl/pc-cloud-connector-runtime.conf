[Service]
Environment=PYTHONPATH=/mnt/d/Hermes/hermes-agent/.fabric-current
# Kanban resolves HERMES_BIN before PATH. The legacy PATH wrapper clears
# PYTHONPATH, making each worker import the slow Windows-mounted mirror.
Environment=HERMES_BIN=/mnt/d/Hermes/hermes-agent/venv/bin/hermes
